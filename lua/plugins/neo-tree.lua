return {
  {
    "nvim-neo-tree/neo-tree.nvim",
    opts = {
      window = {
        position = "right", -- Set the position to "right" instead of "left"
        width = 40, -- Optionally, adjust the width of the Neo-Tree window
      },
    },
  },
}
